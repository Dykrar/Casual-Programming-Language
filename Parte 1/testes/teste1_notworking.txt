#teste 1  not working

decl max (a:Int, b:Int):Int


def max (a:Int, b:Int):Int {
    if a > > b          # > extre && falta "("
      return a          # falta ;
    }
    return b            # falta ;
  }